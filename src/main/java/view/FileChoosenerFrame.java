package view;/*
package view;

import javax.swing.*;
import java.awt.*;

*/
/**
 * FileChoosenerFrame
 *//*

public class FileChoosenerFrame extends JFrame {
    Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();
    public static int sizeWidth = 640;
    public static int sizeHeight = 480;
    public int locationX = (screenSize.width - sizeWidth) / 2;
    public int locationY = (screenSize.height - sizeHeight) / 2;

    public FileChoosenerFrame() throws HeadlessException {

        setTitle("FileChoosenerFrame");

        setBounds(locationX, locationY, sizeWidth, sizeHeight);
FileChooser fc = new FileChooser();

        //addFileChoosener(getContentPane());
        getRootPane().setBackground(Color.orange);
        setVisible(true);
        setDefaultCloseOperation(WindowConstants.DISPOSE_ON_CLOSE);


    }

//    private void addFileChoosener(Container contentPane) {
//        JPanel p = new JPanel(new GridLayout());
//
//        FileChooser fileChoosener = new FileChooser();
//        p.add(fileChoosener);
//
//        contentPane.add(p);
//    }

}
*/
